# Railgun

Railgun is a functional programming language that compiles to C/CUDA. Map and filter operations are automatically parallelized via CUDA.

use 'source ./ENV' to setup environment on v-test-t620
